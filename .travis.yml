sudo: required
services:
  - docker

before_install:
  - docker build -t edwardsmith/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true edwardsmith/react-test npm test

after_success:
  - docker build -t edwardsmith/multi-client ./client
  - docker build -t edwardsmith/multi-nginx ./nginx
  - docker build -t edwardsmith/multi-server ./server
  - docker build -t edwardsmith/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push edwardsmith/multi-client
  - docker push edwardsmith/multi-nginx
  - docker push edwardsmith/multi-server
  - docker push edwardsmith/multi-worker